export enum ROLES {
  ADMIN = "ADMIN",
  NORMAL = "NORMAL",
}

export enum GENDER {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum EventType {
  FORGOT_PASSWORD = "FORGOT_PASSWORD",
  EMAIL_VERIFICATION = "EMAIL_VERIFICATION",
}
